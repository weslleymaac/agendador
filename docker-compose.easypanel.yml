# Um único container: API + Next (ver Dockerfile e docker-entrypoint.sh).
services:
  app:
    build: .
    container_name: agendador-app
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      REDIS_URL: redis://redis:6379
      APP_TIMEZONE_OFFSET: -3
      CRON_SECRET: ${CRON_SECRET:-}
      LOGIN_USERNAME: ${LOGIN_USERNAME:-Admin}
      LOGIN_PASSWORD: ${LOGIN_PASSWORD:-admin}
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "-s", "--max-time", "5", "http://127.0.0.1:3000/health"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 120s
    restart: unless-stopped

  cron:
    image: alpine:3.19
    container_name: agendador-cron
    environment:
      CRON_SECRET: ${CRON_SECRET:-}
    entrypoint: /bin/sh
    command:
      - -c
      - |
        apk add --no-cache curl
        while true; do
          curl -fsS -H "x-cron-secret: $$CRON_SECRET" http://app:3000/api/cron-process-jobs || true
          sleep 60
        done
    depends_on:
      app:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: agendador-redis
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

volumes:
  redis_data:
